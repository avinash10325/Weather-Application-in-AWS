export const kelvinToCelsius = (k) => (Math.round(k - 273.15))

export const kelvinToFahrenheit = (x) => (Math.round(((x -273.15) * 1.8) + 32))

export const metreSecToMileHour = (m) => (Math.round(m * 2.23694))
